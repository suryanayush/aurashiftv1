# AuraShift Backend Environment Variables

# Application Environment (development, production, test)
NODE_ENV=development

# Server port
PORT=3000

# MongoDB connection string
# Local: mongodb://localhost:27017/aurashift
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/aurashift
MONGODB_URI=mongodb://localhost:27017/aurashift

# JWT Secret - MUST be changed in production
# Generate with: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# JWT expiration (1h, 7d, 30d)
JWT_EXPIRE=7d

# Password hashing rounds (10-12 dev, 12-15 prod)
BCRYPT_ROUNDS=12

# Frontend URLs for CORS (comma-separated, optional)
# FRONTEND_URL=http://localhost:3000,http://localhost:19006
